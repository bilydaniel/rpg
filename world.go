package main

/*

type World struct {
	CurrentTilemap *Tilemap
	Tilemaps       []*Tilemap

	CurrentLevel *Level
}

func InitWorld() (*World, error) {
	//TODO remove
		tilemap := InitTilemap()
		err := tilemap.LoadTestMap("floor", asset)
		if err != nil {
			return nil, err
		}

	return &World{
		CurrentTilemap: &tilemap,
	}, nil
}
*/
